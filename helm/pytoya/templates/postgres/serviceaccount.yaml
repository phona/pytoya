{{- if .Values.postgres.enabled }}
{{- $ns := default .Release.Namespace .Values.global.namespace -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "pytoya.fullname" . }}-postgres
  namespace: {{ $ns }}
  labels:
    {{- include "pytoya.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
